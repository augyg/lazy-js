module FFI where


if we have a JS script, with the JSFFIParse module, we can demand the value for some variable or namespace
and either use the value (is really expression) or the function (or object.function) and pass vars that fit
the 'class ToJS '
